import React from 'react';
import PropTypes from 'prop-types';

const NavBar = (props) => {
  return (
    <div>
      navbar
    </div>
  );
}

// NavBar.propTypes = {
//   : PropTypes.
// };

export default NavBar;
